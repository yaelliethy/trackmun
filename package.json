{
  "name": "trackmun",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "deploy:worker": "pnpm --filter @trackmun/worker run deploy"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
